# Data Export Format

The complete Reimbursd export and restore format is planned for Milestone 4 and is not yet
implemented.

The versioned open format will contain structured JSON records, optional original attachments, and
checksums. CSV export will cover common expense fields. Plain exports will not be described as
encrypted; encrypted backups are a separate Milestone 5 capability requiring tested authenticated
encryption.
